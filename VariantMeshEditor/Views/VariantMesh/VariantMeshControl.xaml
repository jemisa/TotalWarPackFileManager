<UserControl x:Class="VariantMeshEditor.Views.VariantMesh.VariantMeshControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VariantMeshEditor"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
   
        <StackPanel x:Name="VariantMeshStackPanel">
            <Button x:Name="ControllerMainButton" Click="Button_Click" FontSize="16">Variant mesh</Button>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20"></ColumnDefinition>
                    <ColumnDefinition Width="1*"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <DockPanel Grid.Column="1" x:Name="VariantMeshContainer" LastChildFill="False">
                    <DockPanel DockPanel.Dock="Top">
                        <Label>Lod:</Label>
                        <ComboBox ></ComboBox>
                    </DockPanel>
                    <!-- Added in code-->
                </DockPanel>
            </Grid>
        </StackPanel>
 
</UserControl>
