<UserControl x:Class="VariantMeshEditor.Views.Animation.AnimationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel x:Name="VariantMeshStackPanel">
        <Button x:Name="ControllerMainButton" Click="Button_Click" FontSize="16">Animation</Button>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20"></ColumnDefinition>
                <ColumnDefinition Width="1*"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="1" x:Name="VariantMeshContainer" LastChildFill="False">
                <DockPanel DockPanel.Dock="Top">
                    <Label>SkeletonName:</Label>
                </DockPanel>
                
                <DockPanel DockPanel.Dock="Top">
                    <Label>Animation:</Label>
                    <ComboBox ></ComboBox>
                </DockPanel>

                <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" HorizontalAlignment="Center">
                    <Label>Frame:</Label>
                    <Border BorderBrush="{x:Null}" >
                        <TextBox x:Name="CurrentFrameTextBox" IsReadOnly="true" TextWrapping="NoWrap" Text="12" VerticalAlignment="Center" />
                    </Border>
                    <Label>/</Label>
                    <Label x:Name="MaxFramesLabel">30</Label>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" HorizontalAlignment="Center">
                    <Button x:Name="NextFrameButton" FontSize="20">⏮︎</Button>
                    <Button x:Name="PlayPauseButton" FontSize="20">⏯︎</Button>
                    <Button x:Name="LastFrameButton" FontSize="20">⏭︎</Button>
                </StackPanel>

                <StackPanel></StackPanel>
                <!-- animation list Added in code-->
            </DockPanel>
        </Grid>
    </StackPanel>
</UserControl>
