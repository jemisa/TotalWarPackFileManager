<UserControl x:Class="VariantMeshEditor.Views.EditorViews.Util.BrowsableItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VariantMeshEditor.Views.EditorViews.Util"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <DockPanel DockPanel.Dock="Top" MaxHeight="50">
            <Label x:Name="LabelName" DockPanel.Dock="Left">LabelName:</Label>
            <Button x:Name="RemoveButton" DockPanel.Dock="Right" Width="70" >Remove</Button>
            <Button x:Name="BrowseButton" DockPanel.Dock="Right" Width="70" >Browse</Button>
            <Border BorderBrush="{x:Null}" >
                <TextBox x:Name="PathTextBox"  TextWrapping="NoWrap" Text="" VerticalAlignment="Center"/>
            </Border>
        </DockPanel>
    </StackPanel>
</UserControl>
