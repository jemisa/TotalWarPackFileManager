<UserControl x:Class="VariantMeshEditor.Views.EditorViews.EditorPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VariantMeshEditor.Views.EditorViews" 
             xmlns:treeviewwithcheckboxes="clr-namespace:TreeViewWithCheckBoxes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="0.50*" />
                <RowDefinition Height="5" />
                <RowDefinition Height="0.50*" />
            </Grid.RowDefinitions>

            <GridSplitter  Grid.Row="1" Background="Gray"  HorizontalAlignment="Stretch" />
            <treeviewwithcheckboxes:ImprovedTreeViewWindow x:Name="TreeView" Grid.Row="0" />
            <Grid x:Name="ToolPanel" Grid.Row="2"></Grid>
        </Grid>
    </Grid>
</UserControl>
